\* EIO experiment (2026-09-16): does a lost lease fencing writes remove any of the accepted remote windows?
\* LEASE_FENCES_WRITES = FALSE (SMB3 still-valid handle, or recover_lost_locks=1).
\* SingleWriter alone, halting: one trace, which tells us WHICH window is left.
SPECIFICATION Spec
CONSTANTS
    Owners = {o1}
    Recoverers = {r1}
    PlainRuns = {}
    Cleanups = {}
    Breakers = {b1}
    NoProc = nobody
    P = dir
    LockName = lock
    DirLockName = dirlock
    MaxObjs = 5
    MaxCrashes = 1
    HostCrashes = FALSE
    MaxLeaseExpiries = 1
    Platform = "posix"
    IdentityStrength = "strong"
    LockCapability = "remote"
    SEED_RECOVER_FOREIGN = FALSE
    SEED_RECOVER_UNCERTAIN = FALSE
    SEED_DEAD_AS_BUSY = FALSE
    SEED_RECOVER_UNCERTAIN_CLEANUP_LOCK = FALSE
    SEED_ACQUIRER_UNLINKS_BY_NAME = FALSE
    SEED_RESTART_RELEASES = FALSE
    SEED_MOVE_ASIDE_FOR_UNCERTAIN = FALSE
    SEED_RENAME_OVER_TAKEOVER = FALSE
    SEED_NO_CAPABILITY_GATE = FALSE
    SEED_TORN_AS_FOREIGN = FALSE
    LEASE_FENCES_WRITES = FALSE
    FIX_REMOTE_LEASE_SPEC = FALSE
    defaultInitValue = defaultInitValue
INVARIANT
    SingleWriter
